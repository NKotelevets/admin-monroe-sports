import styled from "styled-components";

import {
  Title,
  MobileContainer,
  LinkText,
  Text,
  Description,
} from "../../common/styles";

export const GetStartedContainer = styled(MobileContainer)``;

export const GetStartedTitle = styled(Title)`
  margin-bottom: 10px;
`;

export const GetStartedDescription = styled(Description)`
  margin-bottom: 30px;
`;

export const GetStartedText = styled(Text)`
  text-align: center;
  margin-top: 16px;
`;

export const GetStartedLinkText = styled(LinkText)`
  display: inline-block;
  margin: 0;
`;

export const CheckboxContainer = styled.div`
  margin: 0 0 24px;
  padding: 0 20px;
`;
