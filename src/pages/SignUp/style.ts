import styled from "styled-components";

import {
  Title,
  MobileContainer,
  LinkText,
  Text,
  Description,
} from "../../common/styles";

export const SignUpContainer = styled(MobileContainer)``;

export const SignUpTitle = styled(Title)`
  margin-bottom: 10px;
`;

export const SignUpDescription = styled(Description)`
  margin-bottom: 30px;
`;

export const SignUpText = styled(Text)`
  text-align: left;
  margin-top: 16px;
`;

export const SignUpLinkText = styled(LinkText)`
  display: inline-block;
  margin: 0;
`;

export const CheckboxContainer = styled.div`
  margin: 24px 0 40px;
`;
