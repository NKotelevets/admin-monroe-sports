import styled from "styled-components";

import { Title, MobileContainer, Description } from "../../common/styles";

export const JoinTeamContainer = styled(MobileContainer)``;

export const JoinTeamTitle = styled(Title)`
  margin-bottom: 10px;
`;

export const JoinTeamDescription = styled(Description)`
  margin-bottom: 30px;
`;
