import styled from "styled-components";

import { Title, MobileContainer, LinkText, Text } from "../../common/styles";

export const SignInContainer = styled(MobileContainer)``;

export const SignInTitle = styled(Title)`
  margin-bottom: 30px;
`;

export const PasswordLinkText = styled(LinkText)`
  display: block;
  text-align: right;
  margin-top: -20px;
`;

export const SignInText = styled(Text)`
  text-align: center;
  margin-top: 16px;
`;

export const SignInLinkText = styled(LinkText)`
  display: inline-block;
  margin: 0;
`;

export const CheckboxContainer = styled.div`
  margin: 24px 0 90px;
`;
