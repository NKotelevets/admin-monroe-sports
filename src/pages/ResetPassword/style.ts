import styled from "styled-components";

import {
  Title,
  MobileContainer,
  LinkText,
  Text,
  Description,
} from "../../common/styles";

export const ResetPasswordContainer = styled(MobileContainer)``;

export const ResetPasswordTitle = styled(Title)`
  margin-bottom: 10px;
`;

export const ResetPasswordDescription = styled(Description)`
  margin-bottom: 30px;
`;

export const ResetPasswordText = styled(Text)`
  text-align: center;
  margin-top: 16px;
`;

export const ResetPasswordLinkText = styled(LinkText)`
  display: inline-block;
  margin: 0;
`;
